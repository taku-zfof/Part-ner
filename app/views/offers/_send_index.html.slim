#send_offer_index.tab-pane.fade
  - @send_offers.each do |offer|

    /削除モーダル
    = render "/offers/NG_modal_send", offer: offer
    .container.card.index_card.my-3.py-3.text-body.bg-light
      .row.justify-content-center

        /相手の画像
        .col-3.col-md-2.px-0.m-auto.d-flex.justify-content-end
          = link_to user_path(offer.job.user) do
            = image_tag offer.job.user.get_image(100,100), class: 'rounded-circle mt-2 img-thumbnail img-fluid'

        /タイトルと内容
        .col-9.col-md-8
          h4.text-center
            strong
              = offer.job.tytle
          h3.text-nowrap
            strong
              = offer.job.user.name
              | へのオファー
          p.border.rounded-8.bg-light.p-3
            = offer.content

        /削除モーダルを開くボタン
        .col-12.col-md-2.d-flex.align-items-center.justify-content-center
          button.btn.btn-danger.btn-rounded[data-toggle="modal" data-target="#sendNGmodal"]
            i.far.fa-trash-alt.fa-2x
